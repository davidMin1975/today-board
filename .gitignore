# OS
.DS_Store

# secrets — Worker는 `wrangler secret` 사용, 파일로 두지 않는다
.env
.env*
.dev.vars
*.token
*.pem
*.key

# Cloudflare wrangler 로컬 상태
notion-sync/.wrangler/

# 대시보드 로컬 백업 스냅샷 (개인 업무 데이터 포함 가능)
today-board-*.json
today-board-sync-*.json
today-board-mobile.json

# 개인 작업 폴더 — 이 저장소에 절대 커밋 금지
Projects/
Scheduled/
Artifacts/
